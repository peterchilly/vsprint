# 深度学习框架安装

> **所属阶段：** 阶段一：环境搭建
> **阶段目标：** 搭建稳定、可复现的深度学习开发环境，确保后续所有阶段能顺利运行。
> **阶段交付物：**
- 可运行的 Python 虚拟环境
- GPU 验证通过
- 项目目录结构搭建完成
- 实验追踪系统就绪

---

## 目标
安装 PyTorch 及相关辅助库，验证 GPU 可用性。

## 任务清单

### 1. PyTorch 安装
- [ ] 根据 CUDA 版本安装对应 PyTorch
- [ ] 验证：`torch.cuda.is_available()` 返回 True
- [ ] 记录 GPU 名称和数量

### 2. 辅助库安装
- [ ] `torchvision`、`timm`
- [ ] `numpy`、`opencv-python`、`pillow`
- [ ] `tensorboard` 或 `wandb`

### 3. 环境验证
- [ ] 所有库可正常导入
- [ ] GPU 张量运算正常

## 验收标准
- [ ] `torch.cuda.is_available()` 返回 True
- [ ] 所有辅助库可正常导入
