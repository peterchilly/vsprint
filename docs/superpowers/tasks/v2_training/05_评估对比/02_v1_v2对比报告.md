# Task 5.2: v1 vs v2 对比报告

**阶段**：05_评估对比
**依赖**：T5.1
**预计时间**：1 小时

## 目标
对比 v1（当前 best_model.pth, EER=0.1553）和 v2（新训练模型）的全部指标，量化改进效果。

## 需要修改/创建的文件
- 创建：`reports_v2/comparison_v1_v2.md`

## 步骤

### Step 1: 在相同测试集上评估 v1 模型
```powershell
cd C:\Users\Administrator\vsprint
python scripts/evaluate.py --config configs/train_config.yaml --checkpoint checkpoints/best_model.pth --output-dir reports_v2/v1_eval
```

### Step 2: 在相同测试集上评估 v2 模型
使用 T5.1 已产出的 `reports_v2/evaluation_data.json`。

### Step 3: 生成对比报告
`reports_v2/comparison_v1_v2.md` 包含：

**指标对比表**：
| 指标 | v1 (epoch 45) | v2 (epoch ?) | 改善 |
|------|---------------|--------------|------|
| EER | 0.1553 | ? | ?% |
| minDCF | ? | ? | ?% |
| pos_mean | 0.16 | ? | ? |
| neg_mean | 0.002 | ? | ? |
| train_acc | 0.1% | ? | ? |
| 训练设备 | CPU | GPU | — |
| 数据增强 | 无 | 有 | — |

**相似度分布对比**：
- v1 和 v2 的正/负样本余弦相似度分布
- 阈值可分性改善

**训练曲线对比**：
- v1 的 EER 退化趋势 vs v2 的 EER 收敛趋势
- pos_mean 变化对比

**结论**：
- v2 是否在所有指标上优于 v1
- 剩余问题和后续改进方向

### Step 4: 实际音频对比
使用之前测试的 VoxCeleb 样本：
- 同一人（id10001 两段录音）的余弦相似度：v1=0.5982 vs v2=?
- 不同人（id10001 vs id10002）：v1=0.0812 vs v2=?

## 产出物
`reports_v2/comparison_v1_v2.md` — 对比报告

## 验证方法
```powershell
python -c "
# 检查报告存在且包含关键对比数据
with open('reports_v2/comparison_v1_v2.md', 'r', encoding='utf-8') as f:
    content = f.read()
    assert 'EER' in content
    assert 'pos_mean' in content
    assert 'v1' in content and 'v2' in content
    assert '改善' in content
print('PASS')
"
```

## 通过标准
- [ ] 对比报告存在
- [ ] 包含 v1 和 v2 的 EER/minDCF/pos_mean/neg_mean 对比
- [ ] 包含训练曲线对比
- [ ] v2 的 EER 优于 v1
- [ ] v2 的 pos_mean 优于 v1
- [ ] 包含实际音频对比结果
- [ ] 包含结论和后续改进建议
