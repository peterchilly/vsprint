# Task 3.2: 损失函数参数调优

**阶段**：03_模型修复
**依赖**：T1.3, T3.1
**预计时间**：3 小时

## 目标
通过小规模实验（3 epoch）搜索最优的 AAM-Softmax margin 和 scale 组合。

## 需要修改/创建的文件
- 创建：`scripts/loss_param_search.py`
- 创建：`reports/diagnostic/loss_param_search.json`

## 步骤

### Step 1: 编写参数搜索脚本
```python
# scripts/loss_param_search.py
# 参数网格：
margins = [0.1, 0.2, 0.3]
scales = [16, 24, 32]
# 共 9 组实验

# 每组实验：
# 1. 创建模型 + AAM-Softmax(margin=m, scale=s)
# 2. 训练 3 个 epoch（使用 v2 配置的子集）
# 3. 记录每个 epoch 的 EER、loss、train_acc
# 4. 保存结果到 JSON
```

### Step 2: 执行参数搜索
```powershell
cd C:\Users\Administrator\vsprint
python scripts/loss_param_search.py --config configs/train_config_v2.yaml --epochs 3
```

### Step 3: 分析结果
从 9 组实验中选出：
- 3 epoch 后 EER 最低的组合
- train_acc 最高的组合
- loss 下降最稳定的组合

### Step 4: 生成 JSON 报告
```json
{
  "experiments": [
    {"margin": 0.1, "scale": 16, "epoch_3_eer": 0.12, "epoch_3_acc": 0.15, "loss_trend": "decreasing"},
    ...
  ],
  "best_params": {"margin": 0.2, "scale": 32, "reason": "lowest EER after 3 epochs"},
  "recommendation": "Use margin=X, scale=Y for full training"
}
```

## 产出物
- `scripts/loss_param_search.py` — 参数搜索脚本
- `reports/diagnostic/loss_param_search.json` — 搜索结果

## 验证方法
```powershell
python -c "import json; d=json.load(open('reports/diagnostic/loss_param_search.json')); print(len(d['experiments']), d['best_params'])"
# 预期输出：9 和最优参数
```

## 通过标准
- [ ] 参数搜索脚本存在且可运行
- [ ] JSON 包含 9 组 margin×scale 的结果
- [ ] 每组包含 3 epoch 的 EER/loss/acc
- [ ] 选出最优参数组合
- [ ] 最优 3-epoch EER < 0.15
